# verify-windows.exe on GitHub windows-latest (real Windows), 2026-09-18
# run https://github.com/daoxiang0520/uuyc-wine-archlinux/actions/runs/35328758549
# Built twice (MSVC and MinGW) and run twice; both produce identical output.
# The first block below is one of the two; the second was removed as a duplicate.

verify-windows -- window property store out-parameter behaviour
host: Windows 6.2 build 9200, x64

=== 0. obtaining a property store ===
  SHGetPropertyStoreForWindow(console       ) -> 0x80070057  store=0000000000000000
  SHGetPropertyStoreForWindow(desktop       ) -> 0x00000000  store=000001F0D2709680

=== 1. GetValue with a poisoned PROPVARIANT (the actual question) ===
  GetValue(PKEY_AppUserModel_ID) -> 0x00000000
  vt        = 0
  pwszVal   = 0000000000000000
  VERDICT: initialised to VT_EMPTY -- Wine diverges; the patch is right

=== 1b. GetCount / GetAt on the same store ===
  GetCount -> 0x80004005  count=0   (written)
  GetAt    -> 0x80004005  pid=2880154539   (untouched)

=== 2. control: can the store return a value that was set on it? ===
  SetValue -> 0x00000000
  GetValue -> 0x00000001  vt=31  value="Verify.AUMID.Roundtrip"
  control OK: the store works, so test 1's result is meaningful

=== 3. GetCurrentApplicationUserModelId, no explicit AUMID set ===
  (len=NULL)        -> 15703   len=0
  (len=256, buffer) -> 15703   len=256
  Wine returns 15703 (APPMODEL_ERROR_NO_APPLICATION) for both

=== 4. after SetCurrentProcessExplicitAppUserModelID ===
  Set...ExplicitAppUserModelID("Verify.Explicit.AUMID") -> S_OK
  Get...ExplicitAppUserModelID -> 0x00000000  value="Verify.Explicit.AUMID"
  GetCurrentApplicationUserModelId -> 15703  (no value)

  On Windows the last call should return 0 with "Verify.Explicit.AUMID",
  because an explicitly set id is retrievable. If it fails here while
  the explicit getter succeeds, Wine's two APIs disagree.

=== done ===
